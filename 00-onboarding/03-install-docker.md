# Установка Docker
Go запускается на хосте, зависимости — в Docker Compose. Выбери официальную инструкцию Docker Desktop/Engine после определения ОС и запроси подтверждение. Проверь `docker version`, `docker compose version`, `docker run --rm hello-world`. Объясни image, container и `--rm`.
