# Kafka-compatible messaging

Модель: append-only log, topic, partitions, offsets, consumer group. Сначала наблюдай через CLI из Compose, затем моделируй producer/consumer без предположения «ровно один раз».
