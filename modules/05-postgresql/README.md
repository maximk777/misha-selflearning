# PostgreSQL: недели 8–9
Идём от таблицы и четырёх запросов к конкурентным транзакциям, индексам и WAL. Для практики: `bash scripts/deps-up.sh`, затем `bash scripts/seed-postgres.sh`. После каждой темы объясни use case и покажи результат SQL/`EXPLAIN`.
