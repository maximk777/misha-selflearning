# Redis

Redis — быстрый in-memory datastore, не замена модели владения данными. Пройди: types/TTL → cache-aside → stampede → Pub/Sub → Streams. Каждый шаг: прогноз → команда → bounded failure → починка → пересказ.
