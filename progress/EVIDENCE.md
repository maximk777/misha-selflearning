# Доказательства

## Правила

Записывай только проверяемые доказательства: вывод команды, проходящие тесты, работающий код или принятое устное объяснение. Самооценка, план и обещание не являются доказательством.

## Журнал

### 2026-07-21 — onboarding: окружение

- Ритм: Миша подтвердил 14 часов обучения в неделю.
- Диагностика: `bash scripts/doctor.sh` обнаружил Go 1.24.5, Git 2.39.5, Docker 29.5.3, Docker Compose 5.1.4, Make 3.81, `psql` 14.20 и curl 8.7.1.
- Go: временный модуль успешно выполнил `go run .` и `go test ./...`; результат теста — `ok misha-go/onboarding`.
- Docker: `docker run --rm hello-world` завершился сообщением `Hello from Docker!`.

### 2026-07-21 — Go: синтаксис и первый запуск

- Исходный запуск: `go run .` вывел `Миша, запуск №1` и `Миша, запуск №2`.
- Намеренная поломка: после замены `attempt <= 2` на `attempt < 2` программа вывела только `Миша, запуск №1`.
- Самостоятельная починка: Миша вернул поведение с двумя строками; свежий контрольный `go run .` подтвердил результат.
- Пересказ принят: условие `for` проверяется до выполнения тела на каждой итерации; при `attempt == 2` выражение `2 < 2` ложно.
- Лабораторная `labs/go/01-syntax` выполнена; зачёт GS-1 остаётся следующим шагом.
