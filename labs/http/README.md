# Лабораторная HTTP

Работай только в `01-task-service/starter`: `go test ./...`, затем `go test -race ./...`. Сначала предскажи status/body, запусти, внеси ограниченную поломку, сам восстанови и перескажи. Решения открываются только после доказательства.
