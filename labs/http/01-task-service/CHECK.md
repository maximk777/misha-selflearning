# Проверка

Сданы: `go test ./...`, `go test -race ./...`, созданная задача с `201`, invalid JSON с `400` и JSON `{ "error": ... }`, request ID и объяснение body close/cancellation/shutdown. Открой решение только после собственной попытки.
