# Consistent hashing

Запусти `cd starter && go test ./...`. Прогноз: должен ли один key менять node между вызовами? Поломка: убери сортировку или измени выбор и получи нестабильный тест. Затем объясни, что эта маленькая модель не реализует virtual nodes/rebalance и почему production ring сложнее.
