# Лабораторная: первый запуск Go

> Тема: `modules/01-go-start/01-syntax.md`
> Уровень: `start`
> Время: 20 минут
> Запуск из: `labs/go/01-syntax/starter/`

## Результат за 5–15 минут

```bash
cd labs/go/01-syntax/starter
go run .
```

Ожидаются две строки `Миша, запуск №1` и `Миша, запуск №2`.

## До запуска: прогноз

**Один вопрос:** почему цикл сделает именно две итерации?

## Поломка и самостоятельная починка

Замени `attempt <= 2` на `attempt < 2`, снова запусти программу и зафиксируй одну строку. Верни условие, не копируя готовый ответ.

## Пересказ

**Один вопрос:** в какой момент проверяется условие `for`? Сдача — вывод до/после и ответ в `CHECK.md`.
