# Лабораторная: значения и указатели

> Тема: `modules/01-go-start/08-values-pointers.md`
> Уровень: `core`
> Время: 25 минут
> Запуск из: `labs/go/02-values-pointers/starter/`

## Результат за 5–15 минут

```bash
cd labs/go/02-values-pointers/starter
go test ./...
```

Ожидаются два passing tests: копия меняется отдельно, pointer меняет исходный `Counter`.

## До запуска: прогноз

**Один вопрос:** почему `IncrementValue` возвращает новый результат, но не меняет `original`?

## Поломка и самостоятельная починка

В `IncrementPointer` временно убери `*` у параметра или измени receiver на значение. Получи compile error либо падение test, объясни его и верни рабочую версию.

## Пересказ

**Один вопрос:** что копируется при передаче `*Counter`? Сдача — зелёный test, одна поломка и объяснение.
