# Kafka-compatible labs

Используй broker и CLI из `deploy/compose.yaml`; команды ниже концептуальны и должны быть адаптированы к выбранному image. Не обещай exactly-once без конкретной transactional границы.
