# Лабораторные: concurrency

Из этого каталога запускай `go test ./...` и `go test -race ./...`. Каждая лабораторная: прогноз → запуск → ограниченная поломка → самостоятельная починка → пересказ. Не открывай `solutions/` до честной попытки.
