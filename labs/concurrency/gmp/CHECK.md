# Проверка GMP observation

- [ ] Обе точные команды из `LAB.md` завершаются строкой `all jobs finished`.
- [ ] Для каждого значения `gomaxprocs` сохранены наблюдения минимум трёх запусков.
- [ ] Ученик не объясняет порядок логов как гарантию и не меняет WaitGroup harness.
- [ ] Ученик своими словами различает G, M, P и предел `GOMAXPROCS`.
