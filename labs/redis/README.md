# Redis labs

Compose names/ports смотри в `deploy/compose.yaml`: сначала `redis-cli PING`, затем выполняй конкретную лабораторную. Каждая требует прогноз → запуск → bounded failure → repair → retell.
